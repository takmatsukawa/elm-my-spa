{
  "name": "elm-my-spa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel --public-url /elm-my-spa/ src/index.html",
    "prebuild": "rimraf src.*",
    "build": "parcel build -d . --public-url /elm-my-spa/ src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "node-elm-compiler": "^5.0.1",
    "parcel-bundler": "^1.10.3",
    "rimraf": "^2.6.2"
  }
}
